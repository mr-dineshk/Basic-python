#3x3 uniformly distributed roamdom values b/w 0 to 1
import numpy as np
a=np.random([3,3])
print (a)

# 3x3 array normal distribution mean 0 standard deviation1(mean,standard deviation, array)
np.random.normal(0,1, (3,3))
it may have offset identity fills only the main diagram
